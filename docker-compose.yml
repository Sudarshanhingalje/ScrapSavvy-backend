version: "3.8"

services:
  backend:
    build: .
    container_name: scrapsavvy-backend

    ports:
      - "8080:8080"

    env_file:
      - .env

    environment:
      SPRING_PROFILES_ACTIVE: prod

    restart: always
